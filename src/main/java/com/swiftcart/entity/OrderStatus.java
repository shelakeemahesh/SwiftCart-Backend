package com.swiftcart.entity;

public enum OrderStatus {
    PENDING,
    CONFIRMED,
    PROCESSING,
    DISPATCHED,
    OUT_FOR_DELIVERY,
    DELIVERED,
    CANCELLED,
    RETURN_REQUESTED,
    RETURNED
}
